ops:
  carbon_cpu_enriched_sequences:
    config:
      validation_level: auth
      streaming: true
      batch_size: 5000
      rows_per_shard: 50000
      compression: zstd
      output_dir: ".dagster_hf_storage/final_enrichment"
      enable_progress: true